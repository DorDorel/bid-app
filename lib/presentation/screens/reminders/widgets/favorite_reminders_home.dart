import 'package:flutter/material.dart';

class FavoriteRemindersHome extends StatelessWidget {
  const FavoriteRemindersHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
